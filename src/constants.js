import { createRef } from "react";

export const section1 = createRef();
export const section2 = createRef();
export const section3 = createRef();
export const section4 = createRef();

export const primary = '#2aa8f4';
export const secondary = '#00dca8';

export const fideleaseApi = 'http://localhost:5000/api';